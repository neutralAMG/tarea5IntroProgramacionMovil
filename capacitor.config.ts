import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tarea5IntroProgramacionMovil',
  webDir: 'www'
};

export default config;
